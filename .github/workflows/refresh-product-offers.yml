name: Refresh product offers

on:
  schedule:
    - cron: "15 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh-product-offers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Refresh live offers
        run: npm run refresh:product-offers:force

      - name: Run price assurance agent
        run: npm run audit:product-offers

      - name: Typecheck
        run: npm run typecheck

      - name: Keep generated TypeScript cache out of commits
        run: git checkout -- tsconfig.tsbuildinfo

      - name: Commit refreshed data
        run: |
          if git diff --quiet -- data/generated-product-offers.json data/product-page-cache.json data/product-offer-audit.json; then
            echo "No offer changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/generated-product-offers.json data/product-page-cache.json data/product-offer-audit.json
          git commit -m "Update product offers"
          git push
